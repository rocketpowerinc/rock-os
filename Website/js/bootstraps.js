import { startProfileMarkdownSection } from './profile-workspace.js';

startProfileMarkdownSection({
    key: 'bootstraps',
    label: 'Bootstraps',
    emptyLabel: 'bootstrap files',
    searchStatusId: 'bootstrapsSearchStatus',
    searchInputId: 'bootstrapsSearchInput',
    refreshButtonId: 'refreshBootstrapsBtn'
});
